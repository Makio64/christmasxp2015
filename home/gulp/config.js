var src = "./src/"

module.exports = {
  paths: {
	  build: "./build/",
	  dist: "./dist/",

    src: src,

    scripts: src + "scripts/",
    views: src + "views/",
    styles: src + "stylus/"
  }
}
